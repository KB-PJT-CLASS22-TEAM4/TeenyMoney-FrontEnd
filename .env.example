# 백엔드 API 주소. 개발 모드(vite dev)에서는 vite.config.js의 proxy가 /api를 대신 처리하므로
# 비워둬도 되고, 빌드·배포 시에는 반드시 필요하다.
VITE_API_BASE_URL=

# 로그인 화면 배경 영상이 올라간 CloudFront 주소. 끝에 슬래시를 붙이지 않는다.
# 예) https://d1ejjofp0lt569.cloudfront.net/login/v1
# 비어 있으면 영상 대신 정적 이미지(login-family.png)로 폴백한다.
VITE_LOGIN_VIDEO_BASE=
