# Database Scripts

Keep reproducible database changes here.

- `schema.sql`: table, index, constraint, and view definitions.
- `seed.sql`: non-sensitive sample data for local development.

Do not commit real user data, production data, DB passwords, or private dumps.
